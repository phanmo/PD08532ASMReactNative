import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 16,
    },
    list:{
        paddingVertical: 20,
        marginTop: 10,
    },
    productsList:{
        paddingHorizontal: 10,
    }
});